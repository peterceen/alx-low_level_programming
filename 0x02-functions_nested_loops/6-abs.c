#include "main.h"

/**
 * _abs -the modulo
 *
 * description: this converts all values to absolute values
 *
 * return: Always 0.
 */

int _abs(int c)
{
	int a = -1;
	if (c < 0)
	{
		c = c/a;
	}
	return (c);
}
